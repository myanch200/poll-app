%p{:style => "color: green"}= notice
= render @choice
%div
  = link_to "Edit this choice", edit_choice_path(@choice)
  |
  \#{link_to "Back to choices", choices_path}
  
  \#{button_to "Destroy this choice", @choice, method: :delete}
